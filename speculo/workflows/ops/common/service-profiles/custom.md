# custom 服务档案

未知服务类型（包括用户写的 lauctric）保留原名，不猜成另一个产品。Agent 读取用户实际仓库、版本、启动、数据路径、认证和备份恢复说明，生成专用部署 spec。

能运行 Compose 或 native 并不意味着已有逻辑资源分配/恢复 adapter。未知服务资源可通过 existing + 实际授权健康检查登记，或者在受审阅的 host/project脚本中明确实现后再使用；不能伪造“内置支持”标签。
