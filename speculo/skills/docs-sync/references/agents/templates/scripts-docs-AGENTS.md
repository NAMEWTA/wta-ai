# <dir-name>

## Scope

- 说明当前文件只覆盖此脚本/文档根目录。
- 明确子目录不再下钻生成子级 AGENTS.md。

## Module Positioning

- 标记当前角色为 `scripts-docs`。
- 说明这是项目脚本资产或文档的总索引，不是标准的 manifest 模块。

## Directory Structure

- 列出一级子目录与关键根级文件。
- 区分可执行脚本、配置模板、SQL/迁移、任务清单、样例数据与运行时数据目录。

## Responsibilities

- 说明当前目录负责哪些资产索引、阅读路径与维护约束。
- 明确不负责子目录独立文档拆分。

## Core Assets

- 概括各类子树的用途（可按 3-5 类分组）。
- 说明关键入口文件与常见使用场景。

## Key Entry Points

- 指出优先查看的脚本、配置、入口文件与样例。

## Usage And Change Notes

- 先区分“可直接执行的管理脚本”“数据资产”“配置模板”“运行时数据目录”，不要把它们混写成同一类入口。
- 修改此目录内容时，只维护根级 AGENTS.md，不要为子目录继续生成文档节点。
- 涉及运行时数据目录（如 `data/`、`log/`、`store/`）时，只写用途说明，不把运行时数据目录当成文档模块。

## Investigation Rule

- 先用当前索引定位子树与入口。
- 如果当前文件不足以回答问题，继续深入对应脚本、配置或数据文件。
- 文档与文件内容冲突时，以实际脚本和配置为准。

## Maintenance

- 变更时先更新当前 AGENTS.md。
- 只维护根目录这一层，不为子目录生成子级文档。

## References

- 此目录下的关键脚本、配置与入口文件
