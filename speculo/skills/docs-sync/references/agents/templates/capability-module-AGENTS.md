# <module-name>

## Scope

- 说明当前能力型或业务型 manifest 覆盖的目录范围。

## Module Positioning

- 标记当前角色为 `capability-module`。
- 说明它在整仓中承担的能力或业务职责（能力库、业务模块、集成模块等）。

## Directory Structure

- 列出直接子目录、关键文件、主源码路径、核心分层、资源目录和测试目录。
- 只保留定位问题需要的结构。

## Responsibilities

- 说明当前模块负责的实现范围、契约范围和边界。
- 明确不负责的兄弟模块与上层装配职责。

## Core Logic

- 解释当前模块的主干实现路径。
- 解释最关键的分层、能力点、处理链路或数据流。
- 解释它更偏能力模块、业务模块还是集成模块。

## Key Entry Points

- 导出入口文件或主包路径
- 关键源码目录与核心文件
- 核心资源配置与测试入口

## Usage And Change Notes

- 新增接口、实体、任务或集成能力时，先确认主包路径下的核心分层归属，不要跨层堆逻辑。
- 改核心逻辑、资源配置或内部依赖时，同步检查上游消费方和对应测试入口。
- 如果当前模块被运行型应用装配，涉及对外行为变化时要回看装配层。

## Dependencies And Collaboration

- 列出直接内部依赖与直接消费方。
- 说明当前模块如何被上层应用或兄弟模块使用。

## Investigation Rule

- 明确：先读 manifest，再读主源码路径下最关键的分层，再读资源配置与测试。
- 如果本文件无法支撑排查，继续顺着依赖模块和消费方代码下钻。
- 文档与代码冲突时以代码事实为准。

## Core Commands

- 当前模块的测试与构建命令。

## Verification

- 当前模块最小可执行校验。

## Routing

- 上级 manifest

## References

- manifest 文件
- 主源码路径
- 关键源码目录
- 关键资源与测试入口
