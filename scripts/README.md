# 项目脚本

本目录维护 wta-ai 的开发、质量检查、构建、发布和部署入口。脚本以本仓为根，消费本项目的 frontend、backend 与 release-artifacts。

当前处于设计阶段，尚未实现产品脚本；现有 Speculo 工具的检查命令见[项目 README](../README.md)。后续只登记真实可执行并经过验证的入口，不复制 WTA-plus 的 Maven 或多 App 发布命令。
